# Changelog

## v0.1.9 (2026-07-16)

### 优化
- 菜单栏每分钟自动刷新，任务栏图标时间实时更新（`scheduleUpdates` 从状态驱动调度改为固定 1 分钟间隔）
- 移除旧的状态驱动调度逻辑（`todayAt`、动态间隔计算）和未使用的 `config` 导入

### 修复
- 版本子菜单改为展示 `CHANGELOG.md` 最新版本更新说明，`v0.1.9` 修复了开发构建（`version=dev`）下"暂无更新记录"的问题：改为提取 CHANGELOG 首个版本号渲染子菜单，确保所有构建均能看到更新日志

## v0.1.8 (2026-06-05)

### 修复
- 上班时间检测新增 `Display is turned on`（亮屏）事件识别，避免合盖外接屏场景下 lidopen/loginwindow 事件丢失导致无法打卡

## v0.1.7 (2026-06-02)

### 修复
- 修复临近下班和下班后菜单栏不更新的问题：`scheduleUpdates` 将更新间隔从 `RemainingMinutes+1` 分钟改为固定间隔（工作中每 5 分钟、下班后每 30 分钟），避免半天仅刷新两次

### 优化
- 「修改今日上班时间...」从「今日详情」子菜单移至外层菜单栏，操作更方便
- 「修改今日上班时间...」对话框改为从亮屏记录列表中选取，可选「手动输入」作为兜底
- 「当前版本」从只读文本改为级联子菜单，直接展示 `CHANGELOG.md` 完整更新日志
- `EnsureBundle` 自动将 `CHANGELOG.md` 拷贝到 `.app` bundle 的 Resources 目录
- 点击菜单栏图标时清空缓存并手动刷新状态

## v0.1.6 (2026-06-02)

### 新增
- 菜单栏「今日详情」新增子项「修改今日上班时间...」，支持手动修正上班开始时间
  - 修改后持久化到 `~/.worktime/override.json`，按日期存储
  - 修改后立即刷新菜单栏状态和本周统计
- 本周统计（`GetWeek` / `GetByDate`）同步支持上班时间覆盖，与今日详情保持一致

### 修复
- 下班事件检测阈值由硬编码 `LeaveWindowBeginHour=18` 改为跟随用户配置的下班时间
- `GetByDate` 历史日期 `RemainingMinutes`/`State` 使用 `time.Now()` 计算不准确的问题（改为入参传入）

### 优化
- `pmset` 日志采集从 `tail -n 100` 提升至 `tail -n 1000`，减少高频 lid 开合场景的历史数据丢失
- `getEvents` 增加 double-checked locking，避免并发竞态下冗余 pmset 调用
- `ClearCache` 同时清空 parser 层缓存，确保手动修改后两级缓存一致刷新
- 移除已无引用的常量 `LeaveWindowBeginHour`

## v0.1.5 (2026-06-02)

### 修复
- 修复程序持续运行时，隔天上班时间后状态栏未刷新仍显示「下班」的问题（将一次性定时器改为状态驱动的调度链，根据上班时间智能安排下次刷新）
- 修复同一刷新周期内重复执行 `pmset` 子进程的问题（引入 15 秒结果缓存，避免 `GetToday` 和 `refreshWeek` 重复调用）

### 优化
- 状态刷新改为智能调度：未到检测窗口期零轮询，检测窗口内每 5 分钟一次，下班后静默直至次日检测窗口
- 降低 GC 频率（`GCPercent` 5 → 50），减少 CPU 空转，内存上限保持 6MB
- 清理死代码

## v0.1.4 (2026-05-20)

### 新增
- 工作时间监测时间段可自定义配置（菜单栏 / CLI `--range-begin` / `--range-end`）

### 优化
- 优化菜单栏图标刷新逻辑，减少不必要的系统调用
- 降低后台内存占用

## v0.1.3 (2026-05-10)

### 优化
- 优化应用体积，减小编译产物
- 减低运行时性能开销

## v0.1.2 (2026-05-05)

### 新增
- 菜单栏增加版本号展示

### 优化
- 优化应用启动和退出流程
- 修复 brew service 模式下的进程管理问题

### 变更
- 「迟到」修改为「延迟」，表述更中性
- 后台服务迁移至 brew service 管理

## v0.1.1 (2026-04-28)

### 新增
- 菜单栏右键菜单支持导出 CSV
- 菜单栏支持配置上下班时间

### 修复
- 修复菜单栏权限弹窗问题
- 修复时间检测准确性

### 优化
- 分离 Homebrew tap 仓库
- 优化启动退出逻辑

## v0.1.0 (2026-04-20)

### 新增
- 首个版本发布
- 解析 `pmset -g log` 系统日志自动识别上下班时间
- 菜单栏实时状态显示（预计下班 / 倒计时 / 已下班）
- CLI 查询今日考勤详情
- CLI 查询本周考勤统计
- CLI 导出 CSV 考勤记录（含 UTF-8 BOM，Excel 兼容）
- 基于 launchd 的后台守护进程
- 提供 Homebrew 安装方式
